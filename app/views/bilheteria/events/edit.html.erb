<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Editar Evento</h1>
    <div>
      <%= link_to 'Ver', bilheteria_event_path(@event), class: 'btn btn-outline-primary' %>
      <%= link_to 'Voltar', bilheteria_events_path, class: 'btn btn-outline-secondary' %>
    </div>
  </div>

  <%= render 'form' %>
</div>

<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Editar Ingresso #<%= @ticket.ticket_code %></h1>
    <div>
      <%= link_to 'Ver', bilheteria_ticket_path(@ticket), class: 'btn btn-outline-primary' %>
      <%= link_to 'Voltar', bilheteria_event_tickets_path(@ticket.event), class: 'btn btn-outline-secondary' %>
    </div>
  </div>

  <%= render 'form' %>
</div>
